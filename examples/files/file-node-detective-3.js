var a = require('a');
var b = require('b');
var c = require('c');

if (false) {
  var d = require('d');
} else {
  if (true) {
    var f = require('f');
    if (false) {
      var g = require('g');
    }
  }
}