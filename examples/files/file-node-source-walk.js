var a = require('a');
var b = require('b');
var c = require('c');

if (false) {
  var d = require('d');
} else {
  if (true) {
    var f = require('f');
    if (false) {
      let g = 'g1';
    } else {
      function h () {
        let o = {
          name: 'Bo'
        };
      }
    }
  }
}